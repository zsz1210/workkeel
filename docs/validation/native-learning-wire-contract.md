# Scoped external contract learning review

Date: 2026-09-26. Scope: the retained LESSON-0003 request-value mapping incident,
current offline mapping regression, and the native Learning observer contract.

## Retained evidence

WI-0054 records one historical rejection caused by sending the internal `readOnly`
label where the inspected wire schema required `read-only`. WI-0055 records the
installed schema, explicit mapping and independent enum expectations. WI-0056
records a subsequent successful historical turn with usage attribution. Those
files remain unchanged. Reading them today does not rerun or authenticate the
historical experiment and does not qualify today's live providers.

## Current check

The offline test `Provider-owned wire policy matches the inspected App Server v2
contract` in `test/control-plane-live.test.mjs` passed on this source. Its oracle
is the recorded versioned schema, not a live claim about all current schemas.
The native Learning tests separately cover scoped authorization, explicit source
validation, multilingual discovery, actual-use stages, stale-source blocking,
transitive impact and explicit revalidation without rewriting history.

The applicable lesson is to inspect the relevant contract independently before
claiming compatibility. Here it changes the observer integration checks: expected
API fields, exact totals, missing-value behavior and source changes are asserted
at the HTTP boundary, in addition to the UI component and pure-function checks.
Found, read, application and outcome were recorded for the maintainer's private
native task. Those new task and use records are excluded from this public source
projection. Public tests cover the mechanism; the private reuse report is not
an accessible public task record or a fresh live-provider acceptance claim.

## Exclusions

No new live model experiment, provider setup, automatic retrospective, Skill
activation or inferred token saving is part of this review. It establishes one
bounded local reuse path. It does not prove unbiased retrieval accuracy, general
quality improvement, or time savings; those require matched prospective evidence.
