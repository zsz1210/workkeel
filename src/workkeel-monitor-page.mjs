/** Static shell. All project values enter through textContent, never HTML. */
export function renderMonitorPage(nonce) {
  return String.raw`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Workkeel · Work overview</title>
<style nonce="${nonce}">
:root{color-scheme:light dark;--bg:light-dark(#f4f6f8,#12191f);--surface:light-dark(#fff,#1b252d);--text:light-dark(#18303d,#e3edf4);--muted:light-dark(#516874,#a6bcc9);--line:light-dark(#ccd9e0,#40535f);--accent:light-dark(#086779,#74d6db);--wash:light-dark(#e3f2f2,#203d42);--warning:light-dark(#814313,#ffbf80);font:16px/1.5 system-ui,sans-serif;color:var(--text);background:var(--bg)}*{box-sizing:border-box}[hidden]{display:none!important}body{margin:0}main{max-width:1200px;margin:auto;padding:32px 24px}h1{font-size:32px;letter-spacing:-.025em;margin:8px 0}h2{font-size:23px;margin:0 0 12px}h3{font-size:18px;margin:0 0 8px}p{margin:8px 0}small,.muted{color:var(--muted)}.brand{font-size:13px;font-weight:750;letter-spacing:.12em;color:var(--accent)}.top,.toolbar,.tabs,.row{display:flex;align-items:center;gap:12px;flex-wrap:wrap}.top{justify-content:space-between}.tabs{border-bottom:1px solid var(--line);margin:24px 0 20px;padding-bottom:12px}button,input,select{font:inherit;min-height:44px;max-width:100%;border:1px solid var(--line);border-radius:8px;background:var(--surface);color:var(--text);padding:9px 12px}button{cursor:pointer}button[aria-pressed="true"]{background:var(--wash);border-color:var(--accent);color:var(--accent)}button:hover{border-color:var(--accent)}:focus-visible{outline:3px solid var(--accent);outline-offset:3px}a{color:var(--accent);overflow-wrap:anywhere}select{min-width:160px}#tasks{max-width:650px;flex:1}.toolbar{margin:18px 0}.toolbar label{font-size:14px}.toolbar input{flex:1;min-width:160px}.cards{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:12px;margin:18px 0}.card,.panel,.task-card{border:1px solid var(--line);border-radius:12px;background:var(--surface);padding:18px}.card small{display:block}.value{font-size:26px;font-weight:650;overflow-wrap:anywhere}.grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.task-card{text-align:left;width:100%;min-height:160px;display:block;overflow-wrap:anywhere}.task-card strong{display:block;font-size:18px;margin:9px 0}.task-card .tag{display:inline-block}.tag{font-size:12px;padding:3px 8px;border-radius:5px;background:var(--wash);color:var(--accent)}.attention{border-left:4px solid var(--warning)}.attention .tag{color:var(--warning);background:transparent;padding-left:0}.detail-grid{display:grid;grid-template-columns:1.4fr 1fr;gap:14px}.panel{margin:14px 0;overflow-wrap:anywhere}.panel ul{padding-left:20px}.timeline{padding:0;list-style:none}.timeline li{border-left:2px solid var(--line);padding:0 0 15px 16px}.timeline small{display:block}.table-wrap{overflow:auto;border:1px solid var(--line);border-radius:9px}table{border-collapse:collapse;width:100%;font-size:14px;background:var(--surface)}td,th{text-align:left;padding:12px;border-bottom:1px solid var(--line);vertical-align:top;overflow-wrap:anywhere}th{color:var(--muted)}.table-wrap table{min-width:730px}.note{margin-top:28px;padding-top:16px;border-top:1px solid var(--line);font-size:13px}#status{padding:12px 16px;background:var(--wash);border-radius:8px;margin:18px 0;overflow-wrap:anywhere}#status.error{color:var(--warning)}#detail-title{overflow-wrap:anywhere}#state{color:var(--muted)}.run{margin:18px 0}.empty{padding:32px;text-align:center;border:1px dashed var(--line);border-radius:12px;color:var(--muted)}@media(max-width:700px){main{padding:20px 14px}.cards{grid-template-columns:repeat(2,minmax(0,1fr))}.grid,.detail-grid{grid-template-columns:1fr}.value{font-size:21px}h1{font-size:27px}.tabs{gap:6px}.tabs button{flex:1;font-size:14px}.toolbar input,.toolbar select{width:100%;min-width:0}.top{align-items:flex-start}.task-card{min-height:130px}}
</style><main><header class="top"><div><div class="brand">WORKKEEL / OBSERVER</div><h1>Your work, with evidence.</h1><p class="muted">See what needs attention, what was delivered, and what comes next.</p></div><button id="refresh">Refresh now</button></header>
<nav class="tabs" aria-label="Observer pages"><button id="tab-overview" aria-pressed="true">Work overview</button><button id="tab-detail" aria-pressed="false">Task detail</button><button id="tab-quality" aria-pressed="false">Usage & quality</button></nav>
<div class="row"><span class="tag">Read-only</span><span id="updated" class="muted">Not updated yet</span></div>
<div id="status" role="status" aria-live="polite">Loading recorded work…</div>
<div id="content" hidden>
<section id="overview"><h2>What needs your attention</h2><p class="muted">Recorded blockers and review steps come first. Work is assigned in your Codex conversation.</p><div id="overview-counts" class="cards"></div><div class="toolbar"><label for="search">Search</label><input id="search" type="search" placeholder="Task ID or goal"><label for="filter">Status</label><select id="filter"><option value="all">All tasks</option><option value="attention">Needs attention</option><option value="ready">Ready</option><option value="working">Working</option><option value="review">Review</option><option value="acceptance">Acceptance</option><option value="done">Accepted</option><option value="cancelled">Cancelled</option><option value="unavailable">Unavailable</option></select></div><div id="task-list" class="grid"></div></section>
<section id="detail-page" hidden><div class="toolbar"><label for="tasks">Task</label><select id="tasks" aria-label="Task"></select></div><section id="detail" hidden><h2 id="detail-title" tabindex="-1"></h2><p id="state"></p><div class="panel attention"><h3>Next action</h3><p id="next-action"></p><div id="task-links" class="row"></div></div><div class="detail-grid"><div><section class="panel"><h3>Delivery & acceptance</h3><div id="delivery"></div></section><section class="panel"><h3>Evidence & observed checks</h3><div id="evidence"></div></section></div><section class="panel"><h3>Task history</h3><ol id="timeline" class="timeline"></ol></section></div><h3>Recorded execution</h3><div class="cards" id="cards"></div><div id="runs"></div></section></section>
<section id="quality" hidden><h2>Usage & quality</h2><p class="muted">One row per task. Real work, paired experiments and fixtures stay labeled. No efficiency ranking is inferred from unlike samples.</p><div id="quality-table" class="table-wrap" tabindex="0" aria-label="Scrollable task measurements"></div><p class="muted">Lifecycle time includes waiting, review and downtime. Adapter work covers recorded workflow calls only. Native conversation usage and subscription cost are unknown.</p></section></div>
<footer class="note muted"><p>Task acceptance, workflow state and external observations have separate sources. Recorded activity is not proof that a process is alive.</p><p>Unknown is not zero. Partial usage is not a final total. The page refreshes every 2 seconds while visible. Ctrl-C stops the foreground monitor.</p></footer></main>
<script type="module" nonce="${nonce}">
import {formatCount,formatDuration,formatCost} from '/view.mjs';
const $=id=>document.getElementById(id),token=location.hash.slice(1);history.replaceState(null,'',location.pathname);
let snapshot=null,loading=false,timer,view='overview';
const node=(tag,text)=>{const el=document.createElement(tag);if(text!==undefined)el.textContent=text;return el;};
function card(label,value,note){const el=node('div');el.className='card';el.append(node('small',label));const number=node('div',value);number.className='value';el.append(number,node('small',note));return el;}
function showPage(name){view=name;for(const key of ['overview','detail','quality']){$(key==='detail'?'detail-page':key).hidden=key!==name;$('tab-'+key).setAttribute('aria-pressed',String(key===name));}render();}
function table(headers,rows){const el=node('table'),thead=node('thead'),head=node('tr'),body=node('tbody');for(const label of headers){const th=node('th',label);th.scope='col';head.append(th);}thead.append(head);el.append(thead);for(const values of rows){const tr=node('tr');for(const value of values)tr.append(node('td',value));body.append(tr);}el.append(body);return el;}
function overview(){
 const tasks=snapshot.tasks;$('overview-counts').replaceChildren(...[['Needs attention',tasks.filter(t=>t.needs_attention).length,'Review or inspect'],['In progress',tasks.filter(t=>t.task_state==='build').length,'Recorded task state'],['Accepted',tasks.filter(t=>t.task_state==='done').length,'Local acceptance'],['Tasks',tasks.length,'In this project']].map(x=>card(...x)));
 const query=$('search').value.toLowerCase(),filter=$('filter').value;
 const visible=tasks.filter(t=>(t.id+' '+t.title).toLowerCase().includes(query)&&(filter==='all'||filter==='attention'&&t.needs_attention||t.display_state===filter));
 const active=document.activeElement?.dataset.taskId;$('task-list').replaceChildren();
 for(const task of visible){const button=node('button');button.className='task-card'+(task.needs_attention?' attention':'');button.dataset.taskId=task.id;const tag=node('span',task.needs_attention?'Needs attention · '+task.display_state:task.display_state);tag.className='tag';button.append(tag,node('strong',task.title),node('small',task.id),node('p',task.next_action));button.addEventListener('click',()=>{$('tasks').value=task.id;showPage('detail');$('detail-title').focus();});$('task-list').append(button);if(active===task.id)button.focus({preventScroll:true});}
 if(!visible.length){const empty=node('p',tasks.length?'No matching tasks. Change the search or status filter.':'No tasks yet. Prepare a task brief in your Codex conversation.');empty.className='empty';$('task-list').append(empty);}
}
function detail(){
 const task=snapshot.tasks.find(t=>t.id===$('tasks').value);$('detail').hidden=!task;if(!task)return;
 $('detail-title').textContent=task.title;$('state').textContent='Task: '+task.task_state+' · Coverage: '+(task.coverage??'unavailable');$('next-action').textContent=task.next_action;
 for(const id of ['delivery','evidence','timeline','task-links','cards','runs'])$(id).replaceChildren();
 if(task.read_status!=='available'){$('delivery').append(node('p','Task record unavailable. No previous state is shown as current.'));return;}
 $('delivery').append(node('p','Candidate: '+(task.candidate_revision??'Not delivered')),node('p',task.delivery?.summary??'No implementation handoff recorded.'),node('p','Review: '+(task.review?.judgment??'Not recorded')),node('p',task.review?.summary??'A distinct reviewer checks the delivered candidate.'));
 if(task.closeout)$('delivery').append(node('p',task.closeout.summary));
 const criteria=node('ul');for(const text of task.acceptance_criteria)criteria.append(node('li',text));$('delivery').append(node('h3','Acceptance criteria'),criteria);
 for(const pin of task.evidence)$('evidence').append(node('p',pin.stage+' · '+pin.status+' · '+pin.path));
 if(!task.evidence.length)$('evidence').append(node('p','No acceptance evidence recorded yet.'));
 const obs=task.observation;$('evidence').append(node('p','External/check observation: '+obs.status));
 if(obs.value){$('evidence').append(node('small',obs.value.source+' · '+obs.value.observed_at),node('p',obs.value.note));for(const check of obs.value.checks)$('evidence').append(node('p',check.name+' · '+check.status+(obs.status==='candidate-matched'?' · matching candidate':' · not current-candidate evidence')));
  for(const [kind,label] of [['conversation','Return to Codex task'],['pull_request','Open pull request']]){const url=obs.value.links[kind];const valid=kind==='conversation'?/^codex:\/\/threads\/[a-f0-9-]{36}$/.test(url??''):/^https:\/\/github\.com\/[A-Za-z0-9_.-]+\/[A-Za-z0-9_.-]+\/pull\/[1-9][0-9]*$/.test(url??'');if(valid){const link=node('a',label);link.href=url;link.rel='noopener noreferrer';$('task-links').append(link);}}
 }
 for(const event of task.timeline){const li=node('li',event.action+' · '+event.state);li.append(node('small',new Date(event.at).toLocaleString()));$('timeline').append(li);}
 for(const args of [['Input tokens',formatCount(task.usage.input_tokens),'Recorded operations only'],['Output tokens',formatCount(task.usage.output_tokens),'Operation-scoped usage'],['Adapter work',formatDuration(task.timing.adapter_work_ms),'Includes setup, tools and cleanup'],['Observed cost',formatCost(task.usage.cost_usd),'Not subscription billing']])$('cards').append(card(...args));
 if(task.measurement_errors.length)$('runs').append(node('p','Some workflow records could not be verified. Totals are incomplete; inspect the project journal.'));
 if(!task.runs.length)$('runs').append(node('p','No readable workflow runs. Model, tokens and time are unknown.'));
 for(const run of task.runs){const section=node('section');section.className='run';section.append(node('h3',run.run_id+' · '+run.runner_state),node('p','Recorded attempts: '+run.progress.recorded_attempts+' · Completed: '+run.progress.completed_attempts+' · Unresolved: '+run.progress.unresolved_attempts),node('p','Run wall time: '+formatDuration(run.wall_elapsed_ms)+' (includes waits and downtime)'));const wrap=node('div');wrap.className='table-wrap';wrap.tabIndex=0;wrap.setAttribute('aria-label','Scrollable operation measurements');wrap.append(table(['Operation / state','Selected → runtime','Backend','Input / output tokens','Elapsed'],run.operations.map(op=>[op.operation_id+' / '+op.state,(op.requested_model??'Unknown')+' → '+(op.runtime_model??'Unknown'),op.observed_model??'Unknown',formatCount({complete:op.result_recorded,total:op.usage.input_tokens,known_subtotal:op.usage.input_tokens})+' / '+formatCount({complete:op.result_recorded,total:op.usage.output_tokens,known_subtotal:op.usage.output_tokens}),formatDuration(op.adapter_elapsed_ms??op.observed_elapsed_ms)+(op.adapter_elapsed_ms===null?' (snapshot)':'')])));section.append(wrap);$('runs').append(section);}
}
function quality(){const rows=snapshot.tasks.map(t=>t.read_status!=='available'?[t.id,'Unavailable','Unknown','Unknown','Unknown','Unknown','Unknown']:[t.id,t.observation.value?.sample_kind??'unspecified',formatCount(t.usage.input_tokens)+' / '+formatCount(t.usage.output_tokens),formatDuration(t.timing.adapter_work_ms),formatDuration(t.quality.lifecycle_elapsed_ms),String(t.quality.rework_count),t.quality.review_judgment??'Not reviewed']);$('quality-table').replaceChildren(table(['Task','Sample kind','Input / output tokens','Adapter work','Lifecycle time','Rework','Review'],rows));}
function render(){if(!snapshot)return;overview();detail();quality();}
async function refresh(){
 if(loading)return;clearTimeout(timer);loading=true;$('refresh').disabled=true;
 try{if(!token)throw Error('Access link missing. Reopen the URL printed by the monitor command.');
  const response=await fetch('/api/snapshot',{headers:{Authorization:'Bearer '+token},cache:'no-store',signal:AbortSignal.timeout(8000)});
  if(!response.ok)throw Error(response.status===401?'Access denied. Reopen the original monitor link.':'Snapshot unavailable. Inspect the project journal or wait for writes to settle.');
  snapshot=await response.json();const selected=$('tasks').value;$('tasks').replaceChildren();for(const task of snapshot.tasks){const option=node('option',task.id+' — '+task.title);option.value=task.id;$('tasks').append(option);}if(snapshot.tasks.some(t=>t.id===selected))$('tasks').value=selected;
  $('tasks').disabled=!snapshot.tasks.length;$('status').className=snapshot.complete?'':'error';$('status').textContent=snapshot.tasks.length?(snapshot.complete?'Read-only snapshot. ':'Partial snapshot: inspect marked records. ')+snapshot.legacy_tasks_excluded+' legacy tasks excluded.':'No task-first tasks yet. This monitor does not create tasks.';
  $('updated').textContent='Updated '+new Date(snapshot.read_at).toLocaleTimeString();$('content').hidden=false;render();
 }catch(error){snapshot=null;$('content').hidden=true;$('detail').hidden=true;$('status').className='error';$('status').textContent=error.message+' Previous data is not shown as current.';$('updated').textContent='Not current';}
 finally{loading=false;$('refresh').disabled=false;if(!document.hidden)timer=setTimeout(refresh,2000);}
}
for(const name of ['overview','detail','quality'])$('tab-'+name).addEventListener('click',()=>showPage(name));
$('tasks').addEventListener('change',detail);$('search').addEventListener('input',()=>snapshot&&overview());$('filter').addEventListener('change',()=>snapshot&&overview());$('refresh').addEventListener('click',refresh);document.addEventListener('visibilitychange',()=>{clearTimeout(timer);if(!document.hidden)refresh();});refresh();
</script></html>`;
}
