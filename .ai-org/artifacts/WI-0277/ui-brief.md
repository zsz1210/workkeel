# Existing observer continuation text

UI Designer: agent-yuna. Mode: code-first, selected before Build. This is the
visible projection portion of the already approved context design / Issue 140.
WI-0276 owns core validation. WI-0277 owns only monitor-view text and its tests;
WI-0276 Developer releases before this child implementation is claimed.

Preserve existing layout, typography, light/dark themes, focus and controls. No
new controls, permissions, lifecycle action or organization concepts. The same
Traditional Chinese action text must appear in the task card/detail and copied
handoff. Never echo arbitrary error text, prompts, source contents or thread IDs.

Required states: confirmed interruption (prepare fresh material and plan), existing
successor (inspect it, do not duplicate), changed authority, unconfirmed cleanup,
source drift, cancellation, unknown operation and missing evidence. Preserve
stronger authority warnings and existing fallback actions. Snapshot text is not
execution permission. Unknown state remains conservative.

Verify pure projection with unit checks, current real monitor at 1440/390 widths,
empty/loading/stale/error states through its existing browser verifier, and inspect
screenshots. A separate mockup is unnecessary for this bounded text change.
