# WI-0267 independent QA — candidate 0c35fd4450098f1b5bdc8ae2d2d6fe46554d2df3

Reviewer: Quality & Evaluation Engineer / independent QA `agent-lulu`, Principal `human`, in a runtime distinct from Developer `agent-rikku`. This is the judgment for WI-0267 only. WI-0268 has a separate QA worker; its audit implementation is considered here only as a joined dependency. The review covers the committed candidate against base `c9d5ad7741bcf21de6f55825b42d67988372358f`. Post-commit worktree changes to Temple records, handoffs, logs and browser evidence are evidence or administration, not part of that source candidate.

## Judgment

**Pass for the WI-0267 implementation and bounded-evidence scope, with no blocking source defect found.** The integration owner reported `npm run verify` exit 0 on the exact behavioral candidate, and separate WI-0268 QA also passed. Final lifecycle, Doctor, and normal repository integration remain with that owner. This review does not accept a task, publish, deploy, or qualify general model automation.

## Independent checks

- Inspected the full monitor/CLI/test/docs/package diff and its existing measurement readers. `readMonitorSnapshot` projects task-first records only, caps task count and response size, and reaches no writer, runtime adapter or provider. The local server binds `127.0.0.1`, uses a random bearer capability for the snapshot route, exact Host/Origin and cross-site checks, fixed GET routes, no-store responses, CSP and text-node rendering. Failed reads return 503 and the browser hides old details rather than keeping a green snapshot. A capability URL is local access control, not a defense against a hostile process under the same user account; the ADR and guide say this.
- Ran `node --test test/workkeel-monitor.test.mjs`: 3/3 passed. It covers formatter unknown/partial behavior, protected API, no-write project fingerprints, corrupt journal failure, and empty/uninitialized project handling. Independently reran `node ../development-router/formatter-checks.mjs src/workkeel-monitor-view.mjs`: 32/32 passed. The candidate formatter SHA-256 is `d610ae93b423af60923547586cbed89110daaaa2a268626f314150c52444c8a9`, equal to the reviewed submission recorded in the pilot.
- Inspected the browser gate's `result.json` and synthetic desktop/mobile captures. Its 16 state/viewport checks passed. The 390px interrupted image labels token subtotals as partial and cost as Unknown; the stale image removes the detail and shows an error. The 1440px completed image separates lifecycle state from run state and selected/runtime/backend values. Running updates and 503 failures were browser-injected projections; completed, interrupted, unobserved and empty came from the synthetic server journal. These are UI checks, not live model or liveness proof.
- Ran `node --test ../development-router/router.test.mjs ../development-router/measured.test.mjs`: 7/7 passed, including the pinned local classifier's no-network/credential-access test, conservative Chinese/ambiguous routing, one-step model-policy match, and repair-stop rule. No new model dispatch was made. Reviewed external personal entry files separately from the candidate package: `route.mjs` SHA-256 `4a8ccc70ceb92bbd56a35cb76e2378d35e4ed1c9ed467de9cf068f0ae7484182`, current `measured.mjs` `cd205dec40dde9504713cee0f8b42db6d9cabfff6f795d834aeba363e077dbdb`, `real-maintenance.mjs` `2140fe43b42bd0236f2b017d2010a37bc14eec568e9464ce76006f3e18a4f552`, README `../development-router/README.md`. These are outside Workkeel and are not packaged framework behavior.
- Inspected redacted fields of immutable implement/review intents and results. Both runs ended `completed` with requested/runtime model match: Luna implementation 63,744 input/856 output tokens and 37.347789208 s adapter work; Sol source review 59,636/1,680 and 59.139308167 s. Both have unknown observed backend and subscription cost. The implement intent recorded a 33% caller-supplied quota observation, review 32%, above the 30% floor; this is not an authenticated spending cap. The result is one small task with distinct implementation/review roles, zero repair dispatches, and no statistical savings inference. The restricted model environment did not run Node checks; the coordinator did.
- The pilot plan pins pre-live `measured.mjs` SHA-256 `098d4ca3ce69c243718a14ff4b19efed7832d6b7134c83c06536aa97212362e5`; current `measured.mjs` differs because prompt-binding and dispatch guards were added after the two live steps. Those guards have offline checks only. The evaluation guide, developer evidence and maintenance summary state this limit, so the live sample is not misrepresented as qualification of the later launcher revision. The older interactive `route.mjs --launch` path remains unqualified.
- README English/Traditional Chinese/Japanese capability rows and the measurement guide link to existing files, retain the experimental subscription label, distinguish the external personal selector from Workkeel's built-in routing, and avoid treating native Codex sessions as measured. Package boundary check in the owner's full verification log reported 493 files, 1,128,760 packed bytes and 4,322,427 unpacked bytes; the four new distributed paths are the two monitor modules, ADR and routing evaluation. No personal launcher, receipt or browser image is in that package list.
- The integration owner's full `npm run verify` finished exit 0 on candidate `0c35fd4450098f1b5bdc8ae2d2d6fe46554d2df3`, reporting 138 test files and 1,416 passing dot markers. I inspected its repository/link/package preflight and log SHA-256 `82b5d6dcfe6b2bdfcd038405706628fa113502f98c12704f4e938b5497fce2ae`; the shell exit and complete count were provided by the owner, not independently rerun. The owner also reported a core-only offline tarball install, init/Doctor and authenticated empty monitor snapshot without optional LangGraph packages. That smoke result is owner-reported evidence, separate from my focused checks.

## Limits and follow-up

The owner added one uncommitted public-documentation follow-up after the pinned candidate: `docs/getting-started/testing.md` now names the focused monitor browser command and accurately describes its synthetic and injected states. I read that 11-line addition; it changes no runtime code, and it is not included in this exact candidate judgment. The legacy `npm run test:browser` was still running when this note was finalized. The monitor visual gate was inspected from its recorded outputs rather than rerun, avoiding changes to its evidence directory. Journal polling is a point-in-time, potentially costly read across tasks/runs; it is not transactional or proof a runner remains alive. `git diff --check` reported only trailing blank lines in the WI-0267 work-order and UI-brief artifacts, with no behavioral defect. No account setting, global default, credential, source implementation or canonical Temple record was changed by this QA review.
