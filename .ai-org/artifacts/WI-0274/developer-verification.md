# Exact-candidate verification

Candidate: a31ce049722281b67708846d3d98ba2b75f67bbc.
Host: Mac mini / arm64, Node 24.20.0, 2026-09-25.

- `node --test test/workkeel-start.test.mjs test/workkeel-process-recovery.test.mjs`:
  seven tests passed, zero failed. Includes hostile Git hook/config injection and
  partial legacy/native initialization regressions after initial QA rejection.
- `npm run verify`: exit 0, all 143 test files completed. Repository and Markdown
  links passed; reviewed package is 501 files. No dependencies installed.
- CLI walkthrough: fresh Git project, inert policy draft, explicit initialization,
  bridge preview/apply, inert brief draft, explicit approved brief preview/apply,
  identical replay, Doctor and Status. Exactly one intake task; no model dispatch.
- Revised retained process exercise: actual owned child SIGKILL, live-lock refusal,
  stopped-lock recovery, locked and uncertain replay refusals, evidence reconciliation,
  fresh-process continuation and completed replay. First/middle/last each wrote once.
  Original stopped journal has null middle result; its later result records actual
  reconciliation. Task remains Build at measurement; workflow completion is not acceptance.
- Candidate product/support bytes match its Git objects. Initial Doctor: 36 pass,
  one pre-existing stale generated parallel-plan warning, zero fail. No parallel
  plan was used. Closeout Doctor is recorded separately.

The independent review ran alongside complete verification, so its frozen log was
partial. This coordinator subsequently observed the complete command exit 0 before
handoff/acceptance. No behavioral files changed after the tested candidate. Later
changes are evidence/lifecycle only and require their applicable fast checks.

The initial candidate's full suite also exited 0 but its independent review failed;
it was not accepted. Both candidate identities and all original failures are retained.
The seven focused cases are not a count of seven production incidents prevented.
