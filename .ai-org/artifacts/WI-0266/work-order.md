# WI-0266 — local evidence cleanup and corrected paired pilot

## Authority and scope

On 2026-09-24 the maintainer asked to continue the proposed cleanup and small
model comparison, following the previously authorized PR integration workflow.
Principal: human; coordinator: agent-mog; Developer: agent-rikku; distinct
independent reviewer: agent-lulu. Use a Standard workflow, not self-acceptance.
This authorizes one new bounded experiment, not replay or relabeling of WI-0265.

## Cleanup specification and acceptance

Use the pinned publication normalization commands with freshly reviewed digests.
The preview covers 47 obsolete terminal/released worktree fields in nine files
and eight home-path occurrences in retained artifacts. These include all 29
blocked home-path findings and one older reviewed local-path finding. Additional
terminal coordinates are minimized by the existing deterministic operation.
No active coordinates are eligible; no active evidence artifact is affected.
Preserve identities, states, outcomes, timestamps, branches, revisions and evidence
references. Retain value-redacted before/after digests and append normalization
events. Do not rewrite Git history, erase failed attempts, edit canonical JSON
manually, or raise the publication scanner's size limit. The oversized event
journal and existing binary-review queue must remain explicitly unqualified by
the ordinary text audit; separately inspect the unchanged-prefix journal.

## Pilot specification and stop

- Three matched synthetic tasks: intervals, dependency readiness and unknown
  usage accounting. Both gpt-6-luna and gpt-6-sol at medium effort; alternate
  model order. Use the corrected fixed oracle and explicitly unavailable Node
  instruction. Independent oracle review is required before live dispatch.
- Maximum 12 work steps total, one repair per task/model after an ordinary
  settled quality failure, 90 seconds per step, 20 minutes from first dispatch.
  Stop on any infrastructure failure, model drift, unresolved intent, time/step
  limit or missing quota. No automatic new experiment after that stop.
- Observe fresh shared weekly quota at plan creation and before each pair.
  Bind a floor of max(30%, starting remaining percentage minus 3 points).
  Do not dispatch at or below the floor. This is a coarse shared-account brake,
  not a precise per-experiment allowance or financial cap. Keep balances local.
- Existing ChatGPT monthly login only; no Astra, paid API, purchase, credit reset,
  external data, tool network, package install or permission widening. Isolated
  synthetic fixtures only; only their src directory is writable by the model.
- Pin source, prompt, starting code, checks and this work order before spending.
  Use immutable intent/result records in a fresh local-only directory. Keep the
  old pilot untouched. Do not run CPU-heavy verification alongside live pairs.
- Report each first-pass/final pass, actual repairs, runtime model confirmation,
  input/output tokens, adapter time and overall wall time. Unknown remains null.
  n=3 per model is diagnostic, not a model ranking or savings qualification.
  Exclude coordinator and reviewer usage from the narrowly labeled step totals.

## Technical design and verification

Keep production source and dependencies unchanged. Update only the manual pilot
harness to bind fresh approval and quota metadata instead of hardcoded WI-0265
authority. Add offline tests for invalid/missing approval and quota-plan binding.
No router/default-policy promotion. Run focused tests and full final-candidate
offline verification, Doctor and distinct independent review. Record source
equality when later changes are evidence-only. There is no UI change.

## Rollback and completion boundary

Reject a stale/active normalization plan before writes. Preserve the exact
original Git revision and normalization digests; a reviewed forward correction
can recover legitimate fields without restoring unnecessary private values.
Do not undo append-only events or alter historical failed observations.
Completion is cleanup plus three pairs or the first explicit pilot stop,
truthful report, independent review and ordinary protected-branch PR merge.
No npm release, tag, deployment, visibility change or further inference follows.
