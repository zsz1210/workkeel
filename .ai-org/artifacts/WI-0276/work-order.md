# Authorized task material and continuation delivery

The maintainer approved the discussed context design on 2026-09-25 and requested completion. Scope: six stage-specific work types, shared prepared material entry, verified confirmed-interruption continuation, proportionate instructions, corresponding offline and bounded live verification, independent candidate review and normal PR integration. No package installation, account/global model change, publication or unrelated work is authorized.

Acceptance: required authority and source material remain available and bound; invalid stage/scope/source combinations fail before model dispatch; review conclusions retain their scope; interrupted operations remain immutable and unknown operations are never replayed; continuation uses current authorization and claim guards and cannot dispatch twice for one recovery. Existing workflow v1 and ordinary free-text callers stay compatible. No organization/Position model is added to task-first UX.

The study uses existing GPT-6 Sol medium settings, six bounded paired tasks and at most 18 model turns including QA/interruption, with unchanged quality gates. Candidate engineering QA is separate and bounded. Preserve failed evidence; no automatic experiment expansion. Local reports exclude secrets, hidden reasoning and private user data. Completion stops at integrated source, evidence and reporting.

Risk: source freshness, scope confusion and duplicate continuation. Preserve original authority/host guards; test drift, unknown result, unsettled runtime, repeated continuation and expired approval. Rollback is reverting the implementation PR; do not erase historical journals or downgrade policies. Existing dependencies are reused.

Packaging consequence within this delivery: `scripts/check-package.mjs` must require the three new modules and two new documents and account for those exact five files (504 to 509). Existing package roots, excluded private evidence, content scanning and 8 MiB ceiling remain. No other active work item owns that path. This does not authorize publication.
