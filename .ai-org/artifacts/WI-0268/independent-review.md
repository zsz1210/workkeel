# WI-0268 independent QA — pass for the exact candidate

Reviewer: agent-lulu, Principal human, distinct from Developer agent-rikku. This is an actual independent Test-stage review of candidate `0c35fd4450098f1b5bdc8ae2d2d6fe46554d2df3` against base `c9d5ad7741bcf21de6f55825b42d67988372358f`. Scope is `src/publication-audit.mjs`, `test/publication-audit.test.mjs`, and the WI-0268 binary review addendum, verifier, unit test, Developer evidence, and integration join. Source and review artifact hashes match the Developer evidence table; the six reviewed implementation/evidence files have no working-tree modifications. This pass is not a release gate, publication authorization, or clearance for future bytes.

## Judgment and evidence

- **Pass, exact candidate and current inventory.** The previous 2,097,152-byte in-memory threshold is unchanged. Larger current regular files use 65,536-byte reads, with 67,108,864-byte file, 4,194,304-byte logical-line, and 10,000-finding per-file limits. Limit overrun yields a blocked inspection failure, without partial findings. The scanner keeps one logical line while hashing the stream; complete lines preserve existing regex match values and fingerprints, one-based line numbers, UTF-8 decoding across read chunks, CIDR suppression, and the existing private-key-header rule's literal-space behavior. NUL encountered late changes the whole file to generic binary review; tentative text findings are discarded. Initial/open/final metadata checks block drift for both small and streamed files. History inspection remains unchanged.
- `node --test test/publication-audit.test.mjs .ai-org/artifacts/WI-0268/binary-review-check.test.mjs`: **13 passed, 0 failed**. The focused suite exercises long cross-chunk tokens, UTF-8 and newline behavior, CIDR, late NUL, file/line/finding bounds, and small/streamed drift. I inspected the source and test diff against the pinned base as well as executing the tests.
- `node .ai-org/artifacts/WI-0268/verify-binary-review.mjs`: **exact-inventory-reviewed**. It reconciled 117 exact tracked binary paths and SHA-256 values: 68 unchanged prior reviewed PNGs, eight new PNGs, 33 clear compressed artifacts, and eight compressed artifacts with 42 exact uppercase redaction-placeholder home-path matches. Seven of those eight have matching prior archive provenance digests; the remaining closeout archive is bound to its addendum digest. Changed, missing, or new paths do not inherit current clearance.
- I opened all eight newly reviewed PNGs at original resolution and found no visible private path, credential, or live-account value. The verifier also checked PNG signatures and absence of text/EXIF chunks. I independently scanned the full decompressed bytes of all 41 compressed artifacts, including TAR headers, for the relevant token/key, private IP, Tailnet, POSIX home-path, and Windows home-path forms. The only matches were the documented 42 POSIX redaction placeholders; Windows home-path matches were zero. TAR entries were 165 regular files and four directories, with no nonzero trailing bytes. These checks report counts only and did not copy sensitive payload text into this record.

## Limits and next owner

- The committed verifier itself checks regular TAR payloads, not header names, and its pattern list omits the scanner's Windows home-path rule. The independent full-decompressed-byte check above closes that observation for **these exact 41 archive digests only**. If the verifier is intended to review a changed or expanded inventory, add Windows-path and archive-header coverage before relying on its claim of all publication patterns.
- Generic publication audit remains `review-required` for 117 binary files. The addendum is exact-inventory review evidence; it does not alter generic audit findings or authorize publication. The eight archives retain 42 literal home-path-shaped redaction placeholders. A future zero-placeholder publication criterion would require forward copies and renewed digest-bound review.
- Full `npm run verify` is the integration owner's separate check and was not duplicated here. No implementation, canonical lifecycle, external system, or binary content was changed by this QA pass. The integration owner handles subsequent lifecycle and release-gate actions.
