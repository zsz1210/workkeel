# Dispatch and observer efficiency

The maintainer approved both recommendations with GO on 2026-09-25, following
the scenario study: shorten repeated dispatch context and reduce observer read
latency. Existing authorization includes implementation, bounded model checks,
verification, independent review and ordinary pull-request integration.

## Scope and acceptance

Retain the complete task contract and original work input byte values; shorten
redundant explanatory strings only. Do not remove scope, authorization, named
Skills, required reading, model confirmation, expiry, sandbox or result semantics.
Keep unknown measurements unknown and never turn runtime completion into acceptance.

Reduce observer latency using bounded concurrent task-summary reads. Keep the
snapshot schema, attention ordering, strict per-task validation, journal rechecks,
attributable failure isolation, and incomplete totals unchanged. No cross-request
cache, authority reuse, schema migration, package installation or global settings.
No UI layout change; verify the existing monitor browser surface for regression.

Acceptance: complete existing behavioral suite and focused safety regressions;
smaller controlled instruction envelope; lower 200-task snapshot median on the
same synthetic fixture versus frozen f206dbff; unchanged semantic output and
failure isolation. Run bounded before/after Sol medium diagnostics on the existing
35-check retry and 30-check handoff tasks, with two pairs each, at most one repair
per cell and 120 seconds per turn. Retain all outcomes and avoid population/cost
claims. One actual distinct candidate review precedes integration; repair/review
only concrete findings within scope. Stop after normal merge and runtime sync.

## Design and risk

Baseline phase timing (one diagnostic read at 200 tasks): listing 59 ms,
measurement indexing 254 ms, sequential task summaries 1861 ms. Bound summary
read fan-out to eight with deterministic batch order, retaining all existing
validation calls and fresh reads. Do not reuse authority across requests. Test
out-of-order read completion, incomplete records and change visibility between
successive snapshots. The view remains point-in-time, not a cross-task transaction.

Keep JSON task/work values and execution_context fields intact; consolidate
duplicate prose while preserving its obligations. Measure envelope bytes and real
model results separately; fewer bytes do not prove lower tokens or latency.

Root coordinates administration and implementation under agent-rikku; actual
separate Sol runtime review supplies agent-lulu's candidate judgment. These are
attributed identities, not provider authentication. Revert the product commit for
rollback; preserve all experiments, failed checks and review evidence.
