# Workkeel final candidate — Developer evidence

Candidate: `6bb9c98e25fe6056621ba4189ce0ed640e8f4438`.
Developer: `agent-rikku`, Principal `human`. This is implementation evidence,
not an independent review or authorization to merge/publish.

## WI-0259 acceptance

The source package is `@zsz1210/workkeel`; the primary command is `workkeel`.
The `temple` alias, legacy launcher, schema IDs, exact old package/version pins,
historical artifacts and current self-hosting operating contract remain.
English, Traditional Chinese and Japanese README now describe task coordination,
with the optional legacy model clearly separated. Architecture, vision, docs index,
contribution/governance entry points, security/conduct branding and release
instructions match source behavior. Existing approved reporting routes remain.

The owner-authorized GitHub rename is completed and verified separately in
`workkeel-rename-observation.md` in this directory. No package was published and
no release, global install, credential setting or model service was changed.

## Exact-candidate verification

- `npm run verify`: exit 0 on the exact candidate; 131 test files, Node.js v24.7.0
  on local macOS. `real 335.35`, `user 1605.39`, `sys 733.67` seconds.
- Complete original log: `workkeel-verify-6bb9c98e.log` in this directory. Its
  compact reporter preserves failure expansion; passing dots do not supply a
  separately observed test-case count. No universal timing or token claim.
- Repository structure and documentation links passed. Actual dry-run package:
  469 files; the original size/path guards remain.
- Focused lifecycle/runtime/branding: 16 cases passed, 0 failures/skips,
  observed 8811.049333 ms before the subsequent onboarding-path correction.
- Corrected installed-package onboarding test: pass, 2700.784083 ms total.
  It packs, installs offline, initializes and reads pinned Doctor/Status without
  a model provider. This test also passes in the final full suite.
- Doctor before candidate freeze: 37 pass, 0 warnings, 0 failures.

The earlier branding self-test failure and the earlier WI-0258 independent
migration rejection are preserved, not converted to success. The new candidate
includes the README migration fix and real initializer regression, plus deriving
the onboarding CLI path from the already-read package name/bin metadata.

Test-slimming main `5ea9b9690afb1a444a50e0e06a5115a510a4b0ca` is a real ancestor.
Its runner, helpers, CI and testing guide are unchanged. Only the current-name
assertion in the workflow test and the installed-package path are naming updates;
old-pin fixtures, offline dependency-lock logic and all assertions are retained.
Native canonical reconciliation retained both histories. WI-0253/0254/0256 remain
semantically identical to accepted main; their key-order changes are generated.

## Review and retained limits

Independent review must inspect naming/pin compatibility, actual installation,
three-language claims, old record preservation and the remote rename observation.
The shared full measurement can support WI-0258 and WI-0259, but each acceptance
judgment remains separate. Any new behavioral edit invalidates this measurement.

Runtime planning is read-only, not automatic execution. Live LiteLLM tools and
gateway behavior, authenticated multi-human operation, distributed coordination,
production deployment and npm publication were not performed. Tool/network/data
enforcement remains with the host. Different Agents under the same Principal are
not independent humans. Rollback is a reviewed revert preserving task history;
a repository-name reversal would need owner authorization, not archive rewriting.
