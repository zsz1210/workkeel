# WI-0258 Workkeel Developer evidence

- Exact behavioral candidate: `3dfb3094716b207cfc0c33a411ee7c506ba7dccf`.
- Prior accepted additive contract: `8165774f97f98e4609dbd021620f1e9852967c36`.
- Provisional test-task baseline: `53d075bcf619f655cc5613046d5ff05b3e8ab767`;
  the separate test task's final main integration is not yet imported.
- Date: 2026-09-23; Node.js v24.7.0, local macOS.
- Developer: agent-rikku, Principal human. This is not independent acceptance.

## Implemented scope

An opt-in task-first project initializes without Positions; native Work Items reuse
the existing directory with a versioned record. Approved contracts remain immutable
while claims, exact candidate delivery, distinct review, same-contract rework and
local acceptance are recorded separately. History and failed attempts are retained.
Explicit conservative Solo migration keeps legacy bytes; current legacy entrypoints
reject task-first mode. Old published binaries do not understand that marker.

Identity is attributed, not authenticated. Environment descriptions and metadata
guards do not sandbox the host. Project Skills, actual runtime requirements and
native/fixed gateway model connection are separate. Read-only Codex model plans
neither start a runtime nor read credentials or modify global configuration.
Automatic dispatch and live LiteLLM tool interaction remain unimplemented/unverified,
not hidden behind an offline fixture success.

## Verification

1. Focused contract/lifecycle/runtime run: exit 0, 21 tests, 21 pass, zero failures
   or skips, observed 8374.17675 ms. Eight are the prior contract tests; thirteen
   belong to this lifecycle/runtime slice.
2. `npm run check`: exit 0; structure and documentation links pass; exactly 469
   packed paths, 4,108,366 unpacked bytes, within the unchanged size/root limits.
3. Exact-candidate `npm run verify`: exit 0, 130 test files; elapsed `real 361.98`,
   `user 1755.37`, `sys 750.98` seconds. Raw output:
   `.ai-org/artifacts/WI-0258/workkeel-verify-3dfb3094.log`.
   Dot output does not establish a case total. The other task explicitly released
   the full-suite window before this run. No universal speed, cost or token claim.
4. Doctor after administration: 36 pass, one stale generated parallel-plan warning,
   zero failures. Rebuild the plan before dispatch. `git diff --check`: exit 0.

The new tests exercise CLI init/create/claim/status/doctor, native API delivery
through distinct review and close, operation replay and competing identical writes,
stale claims, self-review, expired/missing authority, symlink/traversal rejection,
product drift, out-of-scope changes, evidence tampering, dependency acceptance,
failed-review retention, conservative legacy migration and documented examples.

Bounded pre-candidate read-only support identified three defects: whole artifact
directory exemption, insufficient custom-policy migration screening and unverified
dependency evidence/ancestry. The candidate corrects all three with new regressions.
This informational support was not formal QA and did not write or run tests.

## Retained limits and next owner

No package/repository rename, npm publication, model invocation, live gateway,
global installation, remote deployment or main merge occurred in this slice.
New task-first use is limited to ordinary local attributed workflows. Legacy team,
High-Assurance, sensitive data, custom policy and active-project migration remains
unsupported rather than downgraded. Reports are not runtime product inputs.

The independent reviewer must judge this exact candidate, review the new guards
and narrow claims, and may reuse the compatible full result while doing fresh
focused/adversarial checks. The subsequent branding slice remains separate.
Rollback is a normal reviewed code revert while retaining all versioned records.
