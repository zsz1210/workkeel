# Contributor entry candidate

Developer agent-rikku. Frozen product candidate:
`8518841bdb2b0d66529652507cb9d83516d8b813`, based on main `1a3b132d`.
Isolated worktree: `<LOCAL_HOME>/Documents/ChatGPT/temple-team-entry`.

Reproduced old default readiness headline complete despite task_ready:false and
exit 1 on terminal WI-0252. Dedicated default text now shows independent actor
eligibility and task readiness, stable IDs, task/claim ownership, assignment note,
each blocker and actionable responsibility. No task is clearly not checked.
The --json path and common exit classification are unchanged. Other field
commands retain their output. New team-entry guide is linked in all READMEs and
the docs index, with coordinator versus contributor responsibilities separated.
Package adds only that guide (455-file ceiling, unchanged byte/root boundaries).

Four focused readiness tests passed, including CLI JSON equality, exit parity,
planned-owner mismatch, active-claim continuation, multiple terminal blockers,
missing Principal/task, no-task selection, and unchanged fixture file contents.
Initial fast verification: 58 passed; package/link/repository checks passed.
Latest added missing-task assertion passed in the final focused rerun. Guide
version/context commands were executed with real existing IDs (read-only).

Full frozen-candidate verification and distinct bounded judgment remain pending.
No new role eligibility, policy, lifecycle authority, source schema, managed Skill,
provider call or package release is introduced. The old code's readiness API is
reused; this is a presentation change, not proof of human onboarding effectiveness.
