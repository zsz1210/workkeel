# Observer usability and full task measurements

The maintainer approved the proposed observer work and full-task benefit statistics
on 2026-09-25. Existing authorization includes scoped implementation, verification,
distinct actual QA, normal PR merge and Mac mini runtime sync.

## Acceptance and scope

Provide a Traditional Chinese observer with actionable recorded blockers, explicit
snapshot/event freshness, and separate runtime completion, review and acceptance.
Copy a plain-text handoff with goal, approved scope, candidate, verified evidence,
pending actions and measurement limitations, only on an explicit click. Keep the
loopback capability, read-only API, escaping and failure isolation intact.

Derive complete task elapsed time and disjoint lifecycle phase durations from
verified task history, including ongoing work and rework. Show recorded adapter
time and usage separately; never infer human work, saved money or baseline savings.
Invalid chronology is unknown. Keep fixture/experiment/real-work labels visible.
No new collector, durable task schema, model benchmark, dependency or account change.

## Delivery and stop

Standard workflow, standard risk, code-first UI. Developer agent-rikku; actual
independent reviewer supplies agent-lulu's judgment using an isolated GPT-6 Sol
medium review, at most 300 seconds plus one same-scope repair review if necessary.
Use installed dependencies; focused arithmetic/lifecycle tests, both browser gates,
full verification on the exact candidate, Doctor, required CI and ordinary merge.
Stop after canonical runtime synchronization and evidence report. Rollback is a
reviewed revert retaining historical evidence. No publication or new experiment.
